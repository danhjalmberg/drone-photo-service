/**
 * Centralizes compile-time configuration for the application, simulation
 * model, and graphical view.
 *
 * <p>The non-instantiable settings classes group defaults, selectable limits,
 * physical units, visual styling, and derived values by consuming subsystem.
 * These constants are built into the application; they are not preferences
 * loaded from, or persisted to, an external configuration source.</p>
 */
package io.github.danhjalmberg.dronephotoservice.settings;
