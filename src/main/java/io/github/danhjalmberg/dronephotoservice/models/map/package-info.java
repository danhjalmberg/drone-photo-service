/**
 * Loads maps and metadata and converts between spatial coordinate systems.
 *
 * <p>The map model owns original, cropped world, and display images together
 * with runtime metadata. It provides transactional loading and conversions
 * among world meters, world-image pixels, and display pixels.</p>
 */
package io.github.danhjalmberg.dronephotoservice.models.map;
