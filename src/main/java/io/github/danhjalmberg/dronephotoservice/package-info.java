/**
 * Provides the bootstrap entry point for the Drone Photo Service desktop
 * application.
 *
 * <p>The application is composed as a Model-View-Controller system. Domain
 * state and simulation behavior reside in the model packages, application
 * workflows are coordinated by controllers, and Swing presentation is
 * implemented by the view packages. The entry point configures the look and
 * feel before composing the three root objects on Swing's Event Dispatch
 * Thread.</p>
 */
package io.github.danhjalmberg.dronephotoservice;
